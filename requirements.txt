# [default] incluye yt-dlp-ejs (necesario para YouTube desde 2025; evita 403)
yt-dlp[default]>=2023.12.30
mutagen>=1.47.0
python-dotenv>=1.0.0
requests>=2.31.0
flask>=3.0.0
flask-cors>=4.0.0

# Dependencias opcionales para análisis de audio avanzado (solo Linux/macOS)
# Essentia permite clasificación de género mediante análisis de audio
# En Windows no se instala (pip ignora esta línea si sys_platform == "win32")
essentia>=2.1b5; sys_platform != "win32"

